import { styled } from 'styled-system/jsx'
import { listItem, orderedList, unorderedList } from 'styled-system/recipes'

export const UnorderedList = styled('ul', unorderedList)

export const OrderedList = styled('ol', orderedList)

export const ListItem = styled('li', listItem)
