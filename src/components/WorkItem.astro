---
interface Props {
  props: { id: string; title: string; short: string };
}
const { props } = Astro.props;
---

<h3 class="work-item-title"><a href={`#${props.id}`}>{props.title}</a></h3><p>
  {props.short}
</p>
